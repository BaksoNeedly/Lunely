"""Database tables used by the framework authentication/session layer."""

# The physical table names are kept for compatibility with the existing database.
USERS = "chat_users"
PASSWORD_RESET_TOKENS = "framework_route_reset_passwords"
